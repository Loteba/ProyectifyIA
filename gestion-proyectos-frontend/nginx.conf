server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Cabeceras de seguridad básicas (también en HTTP)
  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "DENY" always;
  add_header Referrer-Policy "no-referrer" always;

  # Proxy a backend del compose (servicio "api")
  location /api/ {
    proxy_pass http://api:5000/api/;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # Soporte SPA
  location / {
    try_files $uri /index.html;
  }
}

# PRODUCCIÓN con HTTPS (recomendado):
# - Monta certificados en /etc/nginx/certs/{fullchain.pem,privkey.pem}
# - Habilita este bloque y el de redirección 80->443
#
# server {
#   listen 80;
#   server_name _;
#   return 301 https://$host$request_uri;
# }
#
# server {
#   listen 443 ssl http2;
#   server_name _;
#
#   ssl_certificate     /etc/nginx/certs/fullchain.pem;
#   ssl_certificate_key /etc/nginx/certs/privkey.pem;
#   ssl_protocols       TLSv1.2 TLSv1.3;
#   ssl_ciphers         HIGH:!aNULL:!MD5;
#
#   root /usr/share/nginx/html;
#   index index.html;
#
#   # Seguridad (HSTS solo útil en HTTPS)
#   add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#   add_header X-Content-Type-Options "nosniff" always;
#   add_header X-Frame-Options "DENY" always;
#   add_header Referrer-Policy "no-referrer" always;
#
#   location /api/ {
#     proxy_pass http://api:5000/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Connection "";
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#
#   location / {
#     try_files $uri /index.html;
#   }
# }
